[workspace]
members = [
  "core",
  "cli",
  "utils",
  "web",
  "sp-ipld"
]
exclude = [
  "bytecursor",
  "sp-cid",
  "sp-multihash",
]


[profile.dev]
debug = 2

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"
