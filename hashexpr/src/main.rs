// use atom::Atom;
// use hashexpr::*;
// use span::Span;

fn main() {
  //  println!("{}", Expr::Atom(None, Atom::Hash(blake3::hash(b"a"))));
  // println!("{:?}", parse_char(Span::new("'\'':char")));
  // println!("{}", Expr::Atom(None, Atom::Char('\'')));
  //  println!("{:?}", parse("(lambda x y z => 0d12:int32 + 0d12:int32)"));
  //  println!("{:?}", parse("(def true : Bool = (data (λ P t f => t)))"));
}
